[tool.poetry]
name = "calculator"
version = "0.1"
description = "will add, subtract, multiply and divide"
authors = ["Ahmed_Ali"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
click = "^8.1.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
